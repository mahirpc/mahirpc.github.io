import Education from '@components/Education'

export const metadata = {
    title: 'Education | mahir',
    description: 'Education section',
}

const page = () => {
  return (
    <div className="right-container">
        <Education />
    </div>
  )
}

export default page